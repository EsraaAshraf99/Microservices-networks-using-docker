Command used to run up the containers:
>$ docker compose up -f docker-compose.yaml -d

Ping frontend container from backend
>$ ping frontend -c 4  

Ping backend container from frontend
>$ ping backend -c 4  



Using command line instead of docker compose:
>$ docker network custom-net
>$ docker run -d --name database --network custom-net ubuntu:latest bash 
>$ docker run -d --name BackEnd --network custom-net ubuntu:latest bash 
